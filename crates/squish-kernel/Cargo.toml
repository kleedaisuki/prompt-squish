[package]
name = "squish-kernel"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "GPL-3.0-or-later"
repository = "https://github.com/kleedaisuki/prompt-squish"
description = "Domain-neutral microkernel for prompt-squish commands"

[dependencies]
serde_json = "1.0"
squish-protocol = { path = "../squish-protocol" }
