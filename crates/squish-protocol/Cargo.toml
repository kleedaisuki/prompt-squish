[package]
name = "squish-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "GPL-3.0-or-later"
repository = "https://github.com/kleedaisuki/prompt-squish"
description = "Stable wire types shared by prompt-squish components"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
